server {
    listen 80;
    index index.html;
    root /usr/share/nginx/html;

    location / {
        try_files $uri /index.html;
    }

    location /stream/ {
        proxy_pass http://api:3000/transcode/;
    }

    location /re-serve/ {
        internal;
        alias /app/videos/;
    }
}